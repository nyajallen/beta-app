<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_resources"
            android:icon="@drawable/ic_tips"
            android:title="Parenting Resources" />
        <item
            android:id="@+id/nav_log"
            android:icon="@drawable/ic_log"
            android:title="Child Weekly Log" />
    </group>

    <item android:checkableBehavior="single"
          android:title="Account">
        <menu>
            <item
                android:id="@+id/nav_profile"
                android:icon="@drawable/ic_profile"
                android:title="Profile" />
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_settings"
                android:title="Settings" />
            <item
                android:id="@+id/child_side"
                android:icon="@drawable/ic_child"
                android:title="Go to Child Check-In Page" />
        </menu>
    </item>

</menu>